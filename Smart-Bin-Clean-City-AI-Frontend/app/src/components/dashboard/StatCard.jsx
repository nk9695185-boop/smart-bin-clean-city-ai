import React from 'react';import {ArrowUpRight,Activity,Package,Trash2,Leaf} from 'lucide-react';import {motion} from 'framer-motion';
const icons=[Package,Trash2,Activity,Leaf];export default function StatCard({item,index}){const Icon=icons[index];return <motion.div className="stat-card" initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} transition={{delay:index*.08}}><div className="stat-top"><span>{item.label}</span><Icon size={19}/></div><div className="stat-value">{item.value}</div><div className="stat-bottom"><b>↑ {item.delta}</b><span>{item.note}</span><ArrowUpRight size={15}/></div></motion.div>}
