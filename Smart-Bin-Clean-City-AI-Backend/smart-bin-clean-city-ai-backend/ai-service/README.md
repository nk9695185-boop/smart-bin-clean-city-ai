# AI Service placeholder
Python/FastAPI service will handle waste image classification, illegal dumping detection, overflow prediction and attribution confidence. Keep this service separate from the Node API so models can scale independently.
